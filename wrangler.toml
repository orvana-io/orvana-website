# This file is intentionally minimal.
# Cloudflare Pages build settings are configured in the dashboard:
#   Build command:        npm run build
#   Build output dir:     dist
#
# Do NOT add a [site] or [assets] block here — that triggers wrangler deploy
# instead of a static Pages build.
